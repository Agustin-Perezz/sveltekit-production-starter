{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "npx eslint --no-warn-ignored \"$CLAUDE_FILE_PATH\" 2>&1 || true"
          }
        ]
      }
    ]
  }
}
